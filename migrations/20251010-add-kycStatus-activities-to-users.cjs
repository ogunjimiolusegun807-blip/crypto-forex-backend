module.exports = {
  up: async (queryInterface, Sequelize) => {
    // No columns to add; already handled by previous migration
  },
  down: async (queryInterface, Sequelize) => {
    // No columns to remove; already handled by previous migration
  }
};
